kind: pipeline
name: default

steps:
  - name: build
    image: plugins/docker
    volumes:
      - name: vol
        path: /build
  - name: upload
    image: alpine:3.8
    volumes:
      - name: vol
        path: /build
    commands:
      - ls /build

volumes:
  - name: vol
    temp: {}
