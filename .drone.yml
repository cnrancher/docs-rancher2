kind: pipeline
name: default

steps:
  - name: build
    image: alpine:3.8
    volumes:
      - name: vol
        path: /build
    commands:
      - touch /build/xx
  - name: oss
    image: alpine:3.8
    volumes:
      - name: vol
        path: /build
    commands:
      - ls /build

volumes:
  - name: vol
    temp: {}
